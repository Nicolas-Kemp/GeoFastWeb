version: "3.8"

services:
  api:
    build: ./api
    ports:
        - 80:80
  ui:
    environment:
      PROXY_BASE_URL: "http://localhost:3080"
    build: ./ui
    ports:
        - 3000:3000

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx-app
  #   volumes:
  #     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
  #   ports:
  #     - 80:80
  #   depends_on:
  #     - ui


